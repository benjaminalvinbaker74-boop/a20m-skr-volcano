[gcode_macro FORCE_RAISE_Z]
gcode:
    # Safely clear any faked homing status to avoid coordinate confusion
    M400
    # Force the Z stepper to move UP by 20mm at a safe, slow speed of 5mm/s
    FORCE_MOVE STEPPER=stepper_z DISTANCE=20 VELOCITY=5
